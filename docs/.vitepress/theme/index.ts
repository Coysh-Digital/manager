import DefaultTheme from 'vitepress/theme'
import './custom.css'

/**
 * The default VitePress theme with the product's own tokens over the top.
 *
 * Nothing is overridden structurally. The docs and the marketing site should read as one product,
 * and the cheapest way to get that is the same palette and the same two typefaces, not a
 * reimplementation of a sidebar that already works.
 */
export default DefaultTheme
